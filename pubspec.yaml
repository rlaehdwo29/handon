name: handon_project
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1      # 앱 배포 버전 관리. 앱 업데이트 시 변경 필요.

environment:
  sdk: ^3.5.1

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8           # iOS 스타일 아이콘 라이브러리
  get: ^4.6.5                       # GetX 상태관리 및 의존성 라이브러리 (Route 관리 포함)
  file: ^6.1.4                      # 파일 시스템을 다루기 위한 라이브러리
  http: ^1.1.0                      # REST API 통신 HTTP 요청 라이브러리
  intl: ^0.19.0                     # 날짜, 숫자, 다국어 지원 라이브러리
  platform: ^3.1.0                  # OS 정보 가져오는 라이브러리
  crypto: ^3.0.3                    # SHA, MD5 등 암호화 기능 제공 라이브러리
  flutter_native_splash: ^2.3.1     # 스플래쉬 라이브러리
  shared_preferences: ^2.1.2        # 앱 내부 DB 라이브러리 (Key-Value 방식)
  flutter_platform_widgets: ^3.3.5
  flutter_screenutil: ^5.8.4        # 반응형 UI 개발 라이브러리
  provider: ^6.0.5                  # 기본 상태 관리 라이브러리
  path: ^1.8.3                      # 파일 및 디렉터리 경로 관련 기능 제공 라이브러리
  path_provider: ^2.0.15            # 앱의 로컬 저장소 경로를 서칭 라이브러리
  sqflite: ^2.2.8+4                 # SQLite 로컬 데이터베이스 관리 라이브러리
  logger: ^1.4.0                    # 디버깅을 위한 로깅 라이브러리
  encrypt: ^5.0.1                   # AES 및 RSA 암호화 라이브러리
  json_serializable: ^6.7.0         # JSON 데이터를 Dart 객체로 변환하는 자동화 라이브러리
  json_annotation: ^4.8.1           # JSON 직렬화/역직렬화 시 사용하는 라이브러리
  fluttertoast: ^8.2.2              # 토스트 메시지 라이브러리
  retrofit: ^4.0.1                  # Retrofit 라이브러리
  page_animation_transition: ^1.0.0 # 페이지 전환 애니메이션 라이브러리
  table_calendar: ^3.0.9            # 캘린더 위젯 라이브러리
  progress_dialog_null_safe: ^1.0.7 # 로딩 다이얼로그 (Progress Indicator) 라이브러리
  dropdown_search: ^6.0.1           # 드롭다운 위젯 라이브러리
  scroll_to_index: ^3.0.1           # ListView에서 특정 인덱스로 스크롤할 수 있는 라이브러리
  package_info_plus: ^4.0.0         # 앱의 버전, 패키지 정보 등 가져오는 라이브러리
  mobile_scanner: ^6.0.2            # QR 코드 및 바코드 스캔 라이브러리
  easy_localization: ^3.0.7         # 다국어 번역 라이브러리

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator: '>=5.0.0 <6.0.0'
  build_runner: any
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/image/
    - assets/translations/
    - assets/env/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
android_12:
  # The image parameter sets the splash screen icon image.  If this parameter is not specified,
  # the app's launcher icon will be used instead.
  # Please note that the splash screen will be clipped to a circle on the center of the screen.
  # App icon with an icon background: This should be 960×960 pixels, and fit within a circle
  # 640 pixels in diameter.
  # App icon without an icon background: This should be 1152×1152 pixels, and fit within a circle
  # 768 pixels in diameter.
  image: assets/image/ic_icon.png

  # Splash screen background color.
  color: "#ffffff"

  # App icon background color.
  icon_background_color: "#ffffff"

  # The branding property allows you to specify an image used as branding in the splash screen.
  #branding: assets/dart.png

  # The image_dark, color_dark, icon_background_color_dark, and branding_dark set values that
  # apply when the device is in dark mode. If they are not specified, the app will use the
  # parameters from above.
  #image_dark: assets/android12splash-invert.png
  #color_dark: "#042a49"
  #icon_background_color_dark: "#eeeeee"

  fonts:
    - family: NotoSansKR
      fonts:
        - asset: assets/fonts/NotoSansKR-Thin.ttf
          weight: 100
        - asset: assets/fonts/NotoSansKR-ExtraLight.ttf
          weight: 200
        - asset: assets/fonts/NotoSansKR-Light.ttf
          weight: 300
        - asset: assets/fonts/NotoSansKR-Regular.ttf
          weight: 400
        - asset: assets/fonts/NotoSansKR-Medium.ttf
          weight: 500
        - asset: assets/fonts/NotoSansKR-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/NotoSansKR-Bold.ttf
          weight: 700
        - asset: assets/fonts/Pretendard-ExtraBold.ttf
          weight: 800
        - asset: fonts/Pretendard-Black.ttf
          weight: 900